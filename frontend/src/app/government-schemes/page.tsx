import { simplePage } from "../../lib/pageFactory";

export default function GovernmentSchemesPage() {
  return simplePage("Government Schemes Portal", [
    "Search by category, state, ministry, and eligibility",
    "Apply online with document uploads",
    "Track application status"
  ]);
}
