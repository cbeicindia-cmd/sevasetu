import { simplePage } from "../../lib/pageFactory";

export default function MarketplacePage() {
  return simplePage("Rural Marketplace", [
    "Browse, search, and filter rural products",
    "Secure cart, checkout, and order tracking",
    "Ratings and reviews for trust"
  ]);
}
