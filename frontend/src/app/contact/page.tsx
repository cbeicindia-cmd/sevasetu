import { simplePage } from "../../lib/pageFactory";

export default function ContactPage() {
  return simplePage("Contact", [
    "Email: support@graminudyog.in",
    "Helpline: 1800-000-000",
    "District coordinator network across states"
  ]);
}
